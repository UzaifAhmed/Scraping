vs code terminal  to bash

source venv/Script/activate

scrapy startproject <project-name>

cd <project-name>

scrapy genspider <spider-name> chocolate.co.uk

scrapy list

scrapy shell

fetch('https://www.chocolate.co.uk/collections/all')

response.css('product-item')

products = response.css('product-item')

product = products[0]

product.css('tag.class::text')

product.css('span.price').get()

product.css('span.price').get().replace('<span> xyz </span>').replace('</span>')

product.css('div.product-item-meta a').attrib['href']

` in chocolatespider.py
def parse(self,response):
    products = response.css('product-item')

    for product in products:

        yield{
            'name': product.css('tag.class::text')
            'price': product.css('span.price').get().replace('<span> xyz </span>').replace('</span>')
            'url': product.css('div.product-item-meta a').attrib['href']
        }

`

exit()

scrapy list

scrapy crawl <spider-name>

scrapy crawl <spider-name> -O mydata.csv

scrapy crawl <spider-name> -O mydata.json

fetch('https://www.chocolate.co.uk/collections/all')

respone.css('product-item')

`
next_page = response.css('[rel="next" ::attr(href)]').get()

if next_page is not None:
    next_page_url = 'https://www.chocolate.co.uk'+  next_page
    yield response.follow(next_page_url, callback=self.parse)
`

exit()

scrapy crawl <spider-name> -O mydata.csv
